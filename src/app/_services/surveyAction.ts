import type { SurveyResult } from '@/app/survey/_models/types';

const saveSurveyResult = (input: SurveyResult) => {
  // TODO: DB/외부 API 저장
  // await fetch(...);
  return { ok: true };
};

export default saveSurveyResult;
